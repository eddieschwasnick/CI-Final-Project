# CI-Final-Project

Steps needed to finish project:

Am going to run 30 api calls per prompt

Then I will calculate the average weights for each sector invested in, and will convert it to a single metric index. Then I will do the mean of each index over each prompt.

Can then take the differences of means based on each on the portfolios output, comparing the mean indexes to the control.

Run a t-test in order to test for significance

Lite LLM package to switch between each LLMs
